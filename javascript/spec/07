import { expect } from 'chai'
import fn from '../07-who-wants-a-date.js'

describe('Guess the number', () => {
  'use strict'

  it('Exists', () => {
    expect(fn).to.be.an('object')
  })
  it('Test manually from the console using `node 07-who-wants-a-date.js`')
})
