import { expect } from 'chai'
import fn from '../10-resolving-symlinks.js'

describe('Guess the number', () => {
  'use strict'

  it('Exists', () => {
    expect(fn).to.be.an('object')
  })
  it('Test manually from the console using `node 10-resolving-symlinks.js`')
})
