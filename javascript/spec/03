import { expect } from 'chai'
import fn from '../03-string-indices.js'

describe('Guess the number', () => {
  'use strict'

  it('Exists', () => {
    expect(fn).to.be.an('object')
  })
  it('Test manually from the console using `node 03-string-indices.js`')
})
