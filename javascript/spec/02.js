import { expect } from 'chai'
import fn from '../02-scrab-bag.js'

describe('Guess the number', () => {
  'use strict'

  it('Exists', () => {
    expect(fn).to.be.a('function')
  })
  it('Test manually from the console using `node 02-scrab-bag.js`')
})
