import { expect } from 'chai'
import fn from '../13-braille.js'

describe('Guess the number', () => {
  'use strict'

  it('Exists', () => {
    expect(fn).to.be.an('object')
  })
  it('Test manually from the console using `node 13-braille.js`')
})
