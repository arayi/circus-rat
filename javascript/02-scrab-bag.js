// 2: Scrab-Bag

// In Scrabble, players remove tiles from a finite set. 
// [Frequency and point value of each tile.](http://scrabblewizard.com/scrabble-tile-distribution/)

// Create a function to tell what tiles remain in the bag after a certain set of tiles played. Represent each tile by the letter that appears on it, and blank tiles by underscores (`_`).

// **Input:**

// An uppercase string with the tiles that have already been played.

// E.g., if 14 tiles have been played, you would be given an input like this:

// ```
// AERETOXMYCNS_B
// ```

// **Output:**

// The number of tiles remaining followed by a comma separated list of the tiles themselves.

// For example: 

// ```
// 10: E
// 9: I
// 8: A
// 7: O
// 5: R, N, T
// 4: U, L, D
// 3: G, S
// 2: P, H, F, V, W
// 1: B, Y, J, K, M, Q, C, Z, _
// 0: X
// ```

const fn = (input) => {
  return input
}

export default fn